output {
    if "MIGRATED" in [tags] {

        elasticsearch {
            hosts => ["localhost"]
            index => "%{[@metadata][target_idx]}"
        }

        file {
          path => "/var/log/file_output/migration"
        }

    } else {

        elasticsearch {
            hosts => ["localhost"]
            index => "idx-%{+YYYY.MM}"
        }
        #TODO this should be removed when done debugging
        file {
           path => "/tmp/logstashdebug.txt"
           codec => rubydebug
        }
    }
}
